# Data Structures Utilities

## `sizeof()` Operator 

### One Dimensional Array

* **FORM `sizeof(a)`, RETURN `size_t` \(size of array `a` in bytes\)**
* While `sizeof(a)` returns size of element `a[0]` in bytes
* `length = sizeof(a) / sizeof(a[0])`

